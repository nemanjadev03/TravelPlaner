# Travel Planner Frontend
- React + Tailwind + React Router + Axios + D3
- API base: `REACT_APP_API_URL` (default: `http://localhost:5000/api`)

## Start
```
npm install
# if you change API port, set .env -> REACT_APP_API_URL
npm start
```